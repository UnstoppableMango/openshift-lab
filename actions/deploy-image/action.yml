name: Deploy Container Image
description: Deploy a container image using specified JDK version
inputs:
  name:
    description: 'Name of the container image to deploy'
    required: true
    default: 'my-container-image'
  github-token:
    description: 'GitHub token for authentication'
    required: true

runs:
  using: composite
  steps:
    - name: Deploy the application
      run: echo TODO
      shell: bash
